import { Typography, Box } from '@mui/material';

export default function IngredientsPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Ингредиенты
      </Typography>
      <Typography color="text.secondary">
        Здесь будет справочник ингредиентов. Пока — заглушка.
      </Typography>
    </Box>
  );
}